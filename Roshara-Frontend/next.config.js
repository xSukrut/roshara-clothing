const nextConfig = {
  images: {
    remotePatterns: [
      { protocol: "https", hostname: "roshara-clothing.onrender.com" },
      { protocol: "https", hostname: "roshara.in" },        // optional future
      { protocol: "https", hostname: "www.roshara.in" },
    ],
  },
};
export default nextConfig;

